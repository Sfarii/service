{% extends "@FOSUser/layout.html.twig" %}

{% block fos_user_content %}
  <div class="login_page_wrapper">
    <div class="md-card" id="login_card">
      <div class="md-card-content large-padding" id="register_form">
        <a href="{{ url('fos_user_security_login') }}" class="uk-position-top-right uk-close uk-margin-right uk-margin-top back_to_login"></a>
        <h2 class="heading_a uk-margin-medium-bottom">{{ 'signup.title'|trans }}</h2>
        {{ form_start(form, {'method': 'post', 'action': path('fos_user_registration_register'), 'attr': {'novalidate': 'novalidate','class': 'fos_user_registration_register'}}) }}

        <div class="uk-form-row">
          {{ form_label(form.username) }}
          {{ form_widget(form.username , { 'attr' : {'class' : 'md-input'}}) }}
          <div class="parsley-errors-list filled uk-text-danger text-danger">
            {{ form_errors(form.username) }}
          </div>
        </div>
        <div class="uk-form-row">
          {{ form_label(form.plainPassword.first) }}
          {{ form_widget(form.plainPassword.first , { 'attr' : {'class' : 'md-input'}}) }}
          <div class="parsley-errors-list filled uk-text-danger text-danger">
            {{ form_errors(form.plainPassword.first) }}
          </div>
        </div>
        <div class="uk-form-row">
          {{ form_label(form.plainPassword.second) }}
          {{ form_widget(form.plainPassword.second , { 'attr' : {'class' : 'md-input'}}) }}
          <div class="parsley-errors-list filled uk-text-danger text-danger">
            {{ form_errors(form.plainPassword.second) }}
          </div>
        </div>
        <div class="uk-form-row">
          {{ form_label(form.email) }}
          {{ form_widget(form.email , { 'attr' : {'class' : 'md-input'}}) }}
          <div class="parsley-errors-list filled uk-text-danger text-danger">
            {{ form_errors(form.email) }}
          </div>
        </div>
        <div class="uk-margin-medium-top">
          <button type="submit" class="md-btn md-btn-primary md-btn-block md-btn-large">{% trans from "FOSUserBundle" %}registration.submit{% endtrans %}</button>
        </div>
        {{ form_widget(form) }}
        {{ form_end(form) }}
      </div>
    </div>
    <div class="uk-margin-top uk-text-center">
      <a href="{{ url('fos_user_security_login') }}">{% trans from "FOSUserBundle" %}registration.back{% endtrans %}</a>
    </div>
  </div>

{% endblock fos_user_content %}
