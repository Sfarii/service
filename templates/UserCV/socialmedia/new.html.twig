<button type="button" class="uk-modal-close uk-close"></button>
<div class="uk-modal-header">
  <h3 class="uk-modal-title">{{ 'user_cv.socialmedia.new'|trans }}</h3>
</div>
<div class="uk-animation-fade">
  {{ form_start(form ,{ 'action': path('socialmedia_new') }) }}
  <div class="uk-form-row">
    <label class="uk-form-label">{{ form_label(form.account) }}</label>
    {{ form_widget(form.account)}}
    <div class="parsley-errors-list filled uk-text-danger">
      {{ form_errors(form.account) }}
    </div>
  </div>
  <div class="uk-form-row">
    <label class="uk-form-label">{{ form_label(form.type) }}</label>
    {{ form_widget(form.type)}}
    <div class="parsley-errors-list filled uk-text-danger">
      {{ form_errors(form.type) }}
    </div>
  </div>
  <div class="uk-modal-footer uk-text-right">
    <button type="button" class="md-btn md-btn-flat uk-modal-close">Close</button>
    {{ form_widget(form.submit) }}
  </div>
  {{ form_widget(form) }}
  {{ form_end(form) }}
</div>
